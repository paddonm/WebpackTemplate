export default (App) => {
  // State can be managed with App argument
    // App.state - view application state
    // App.setState - update application state
  
  // Build HTML template to render component
  return ``;
};
