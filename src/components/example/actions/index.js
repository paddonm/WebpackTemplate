export default (App) => {
  // Execute any component actions here
    // Functions will execute AFTER DOM template is mounted (similar to ComponentDidMount)
    // State can be managed with App argument
      // App.state - view application state
      // App.setState - update application state (returns a Promise)
  };
